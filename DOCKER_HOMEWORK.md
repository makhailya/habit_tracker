# 🐳 Домашнее задание: Docker Setup

## 🎯 Что нужно сделать

Опишите файл `docker-compose.yaml` для запуска всех частей проекта единой командой.

## ✅ Все уже готово!

Я создал для вас все необходимые файлы:

### 📁 Основные файлы:

1. **docker-compose.yml** - конфигурация всех сервисов
2. **Dockerfile** - оптимизированный образ приложения
3. **.dockerignore** - исключение ненужных файлов
4. **.env.docker** - шаблон переменных окружения для Docker
5. **DOCKER_SETUP.md** - подробная документация
6. **docker-start.sh** - автоматический скрипт запуска

### 📚 Инструкции:

7. **PR_INSTRUCTIONS.md** - как создать Pull Request
8. **HOMEWORK_CHECKLIST.md** - чеклист проверки перед отправкой

## 🚀 Что делать дальше (пошагово):

### Шаг 1: Скопируйте файлы в свой проект

Все файлы находятся в папке `habit_tracker/`. Замените ими старые версии (если были).

### Шаг 2: Протестируйте Docker локально

```bash
# Перейдите в папку проекта
cd habit_tracker

# Создайте .env для Docker
cp .env.docker .env

# Запустите через скрипт (рекомендуется)
chmod +x docker-start.sh
./docker-start.sh

# Или вручную
docker-compose up -d
```

**Проверьте что работает:**
- http://localhost:8000 - API
- http://localhost:8000/admin - Админка
- http://localhost:8000/swagger - Swagger UI

```bash
# Проверка всех контейнеров
docker-compose ps

# Все должны быть "Up"
```

### Шаг 3: Создайте ветки и коммит

```bash
# 1. Убедитесь что .idea удалена из репозитория
git rm -r --cached .idea  # если она там есть
git commit -m "Remove .idea from repository"

# 2. Создайте ветку develop (если еще нет)
git checkout -b develop
git push -u origin develop

# 3. Создайте ветку для домашнего задания
git checkout -b homework/docker-setup

# 4. Добавьте все Docker файлы
git add docker-compose.yml Dockerfile .dockerignore .env.docker
git add DOCKER_SETUP.md docker-start.sh PR_INSTRUCTIONS.md HOMEWORK_CHECKLIST.md
git add README.md .gitignore

# 5. Сделайте коммит
git commit -m "Add Docker setup for project deployment

- Add docker-compose.yml with all services
- Update Dockerfile with optimizations
- Add .dockerignore for build optimization
- Create .env.docker template
- Add comprehensive documentation
- Add automation scripts
- Update README and .gitignore"

# 6. Отправьте на GitHub
git push -u origin homework/docker-setup
```

### Шаг 4: Создайте Pull Request

**Следуйте подробным инструкциям в `PR_INSTRUCTIONS.md`**

Кратко:
1. Откройте ваш репозиторий на GitHub
2. Нажмите "Compare & pull request"
3. **Base:** `develop` ← **Compare:** `homework/docker-setup`
4. Заполните описание (пример в PR_INSTRUCTIONS.md)
5. Нажмите "Create pull request"
6. Скопируйте ссылку на PR

### Шаг 5: Отправьте преподавателю

Отправьте ссылку на Pull Request для проверки.

## 📋 Проверьте перед отправкой

Используйте **HOMEWORK_CHECKLIST.md** для проверки всех критериев.

Основное:
- ✅ docker-compose.yml работает
- ✅ Все сервисы запускаются
- ✅ .env.docker создан
- ✅ README обновлен
- ✅ .gitignore правильный
- ✅ .idea/ НЕ в коммите
- ✅ .env НЕ в коммите
- ✅ PR создан из homework в develop

## 🎓 Что вы получаете

### Соответствие критериям задания:

1. ✅ **docker-compose.yaml**
   - Файл в корне проекта
   - Все сервисы описаны (db, redis, web, celery, celery-beat)
   - Правильные порты
   - Зависимости настроены
   - Используются переменные из .env

2. ✅ **Переменные окружения**
   - Шаблон .env есть
   - Чувствительные данные вынесены
   - Корректно подключены через env_file

3. ✅ **README**
   - Шаги по запуску описаны
   - Команда запуска есть
   - Проверка сервисов описана

4. ✅ **Git и GitHub**
   - PR из homework в develop
   - docker-compose в репозитории
   - Игнорируемые файлы НЕ в коммите
   - .gitignore настроен

### Бонусы (сверх требований):

- ⭐ Health checks для сервисов
- ⭐ Автоматическое применение миграций
- ⭐ Volumes для данных
- ⭐ Оптимизированный Dockerfile
- ⭐ .dockerignore
- ⭐ Подробная документация (DOCKER_SETUP.md)
- ⭐ Скрипт автоматизации (docker-start.sh)
- ⭐ Restart policies
- ⭐ Безопасность (непривилегированный пользователь)

## 📖 Структура документации

```
habit_tracker/
├── docker-compose.yml          ⭐ Главный файл Docker
├── Dockerfile                  🐳 Образ приложения
├── .dockerignore              📦 Исключения при сборке
├── .env.docker                ⚙️ Шаблон для Docker
├── docker-start.sh            🚀 Скрипт запуска
├── DOCKER_SETUP.md            📚 Подробная документация
├── HOMEWORK_CHECKLIST.md      ✅ Чеклист задания
├── PR_INSTRUCTIONS.md         📝 Инструкция по PR
└── README.md                  📖 Обновлен с Docker секцией
```

## 🆘 Если что-то не работает

### 1. Docker не установлен
```bash
# Mac
brew install docker docker-compose

# Linux
sudo apt-get install docker.io docker-compose

# Windows
# Скачайте Docker Desktop
```

### 2. Порты заняты
```bash
# Измените порты в docker-compose.yml
ports:
  - "8001:8000"  # вместо 8000:8000
```

### 3. База данных не создается
```bash
# Пересоздайте volume
docker-compose down -v
docker-compose up -d
```

### 4. Celery не работает
```bash
# Проверьте логи
docker-compose logs celery
docker-compose logs redis
```

### Подробные решения в DOCKER_SETUP.md

## 💡 Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Пересборка
docker-compose build

# Выполнение команд
docker-compose exec web python manage.py createsuperuser
docker-compose exec web python manage.py test

# Подключение к контейнеру
docker-compose exec web bash
```

## 🎉 Готово!

После выполнения всех шагов:
1. Проект запущен в Docker ✅
2. Pull Request создан ✅
3. Все критерии выполнены ✅

**Отправьте ссылку на PR преподавателю для проверки!**

---

## 📞 Нужна помощь?

- **DOCKER_SETUP.md** - полная документация по Docker
- **PR_INSTRUCTIONS.md** - как создать Pull Request
- **HOMEWORK_CHECKLIST.md** - чеклист перед отправкой
- **README.md** - общая информация о проекте

Удачи! 🚀
